MOJOBOOST CONNECT EVERYTHING PARALLEL ROUND

COPYING INSTRUCTIONS

This file contains complete standalone prompts. Copy exactly one full TASK section into each Claude Code session. Run as many tasks simultaneously as desired in the same shared mojoboost repository. Do not combine tasks in one session. Do not create branches, worktrees, clones, or separate checkouts.

THE PURPOSE OF THIS ROUND IS CONNECTION, NOT INVENTION

Connect existing implementations to production trainers, bindings, public APIs, serialization, packaging, and documentation. Do not create another parallel implementation when one already exists. Do not add speculative subsystems. First locate every existing implementation and handoff, then select one authoritative path, connect it, and retire or clearly quarantine duplicates within your owned lane.

MANDATORY RULES FOR EVERY TASK

- Inspect git status and all relevant existing modules and handoffs before editing.
- Stay strictly inside the files owned by the task.
- Preserve concurrent work. Never revert, overwrite, clean, stage, commit, or reformat another lane's files.
- If a change is required outside your ownership, write the exact requested patch or signature in your owned handoff file. Do not edit the other lane.
- Do not run tests, test suites, Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network services, retry loops, polling loops, watchers, or background processes.
- Do not write new tests in this round. List the smallest later focused checks in the handoff and label every command UNRUN.
- Static inspection only: rg, sed, wc, git status, git diff, and git diff --check are allowed.
- Do not commit, push, publish, upload, tag, dispatch workflows, or change repository settings.
- Keep training, prediction, objectives, metrics, data representation, device policy, and serialization in Mojo. Python remains a thin API and ecosystem adapter.
- Preserve existing CPU and GPU fallback paths behind explicit internal switches until later validation. Do not silently change defaults when a new path is unvalidated.
- Do not claim correctness, parity, performance, packaging, or hardware support without later evidence.
- A feature is not connected merely because it is imported. It must be called by the production path, receive real public parameters, affect model behavior, serialize required state, and surface clear unsupported errors where applicable.
- At completion write the owned handoff with files changed, exact call path connected, old path retained or removed, remaining blockers, cross-lane requests, and minimal later validation commands marked UNRUN.

All tasks are parallel. Numbers are labels, not an execution sequence. Central files have exactly one owner. Other lanes must request central changes through handoffs.


======================================================================
TASK 01: CONNECT THE ENTIRE GPU TRAINING STACK TO THE PRODUCTION TRAINER
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/train_gpu.mojo, src/mojoboost/gpu_runtime.mojo, src/mojoboost/gpu_session.mojo if it exists or is needed, src/mojoboost/histogram_gpu.mojo, src/mojoboost/__init__.mojo, and handoffs/connect_01_gpu_trainer.md. Do not edit specialized GPU modules, generic Tree or split semantics, CPU training, bindings, Python, tests, benchmarks, or documentation outside the handoff.

Connect every production-ready existing GPU subsystem into train_gpu.mojo rather than leaving it as isolated code. Inspect and consume the real interfaces in gpu_active_rows, gpu_leaf_batching, gpu_frontier, gpu_binned_layout, gpu_histogram_specializations, gpu_fused_round, gpu_gradient_stream, gpu_objectives_native, gpu_split_search, gpu_multiclass_batch, gpu_predict, apple histogram/device policy, hybrid scheduling, initialization, and unified-memory policy.

Create one coherent production call graph with one persistent context/session per fit, reusable kernels and buffers, device-resident binned data, predictions, gradients, Hessians, row assignments, frontier state, and validation allocations. Connect active-row ranges so child histograms do not scan unrelated rows. Connect compatible multi-leaf batching. Connect packed bin descriptors. Connect device objective generation and split selection through conservative strategies. Connect multiclass batching where its existing contract matches tree ordering. Connect persistent prediction state for validation reuse.

Every connected stage needs an explicit internal fallback to the established path until later validation. Do not delete the known trainer. Centralize synchronization ownership and eliminate obvious per-node context/allocation creation. Preserve deterministic seeds, bagging, GOSS, feature sampling, monotonic and interaction constraints, missing/categorical routing, histogram subtraction, multiclass output order, and early-stop model truncation.

If two existing modules compete, select the path that preserves current Tree semantics and document the rejected duplicate. If an existing interface cannot be called correctly, do not invent fake glue; document the exact provider change in the handoff and connect all independent stages.


======================================================================
TASK 02: CONNECT ACTIVE ROWS, LEAF BATCHING, FRONTIERS, AND PACKED BINS
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/gpu_active_rows.mojo, src/mojoboost/gpu_leaf_batching.mojo, src/mojoboost/gpu_frontier.mojo, src/mojoboost/gpu_binned_layout.mojo, src/mojoboost/gpu_histogram_specializations.mojo, and handoffs/connect_02_gpu_dataflow.md. Do not edit train_gpu.mojo, histogram_gpu.mojo, objectives, prediction, device policy, bindings, Python, tests, or benchmarks.

Unify these modules into one consumable GPU dataflow contract for Task 01. Active rows must partition into compact child ranges and subsequent child histograms must consume only those rows. Leaf batching must describe multiple compatible leaf jobs without one allocation or synchronization per leaf. Frontier state must own offsets, lengths, statistics, and completion state. Packed bins must truthfully choose 8-bit, 16-bit, or wider storage without losing missing or categorical markers. Histogram specializations must consume the same descriptors.

Remove or quarantine duplicate descriptors and conversion utilities inside owned files. Define explicit allocation ownership, capacity growth, empty-leaf behavior, deterministic partition ordering, multiclass indexing, bagging/GOSS row weights, feature masks, histogram subtraction compatibility, and portable fallbacks. Make interfaces small enough for the central trainer to call without knowing kernel internals.

Do not claim the modules are integrated merely because they import one another. The handoff must provide exact central calls and state transitions for Task 01.


======================================================================
TASK 03: CONNECT FUSED ROUNDS, DEVICE OBJECTIVES, AND DEVICE SPLIT SEARCH
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/gpu_fused_round.mojo, src/mojoboost/gpu_gradient_stream.mojo, src/mojoboost/gpu_objectives_native.mojo, src/mojoboost/gpu_split_search.mojo, and handoffs/connect_03_device_round.md. Do not edit train_gpu.mojo, histogram modules, prediction, Tree, generic objectives, device policy, bindings, Python, tests, or benchmarks.

Unify the existing device-round components into one production-consumable staged interface. Keep predictions, gradients, Hessians, sampling scales, histogram reductions, gain scans, best split, child statistics, and leaf value calculations device-resident when the objective and constraints are supported. Cover only existing sound objective implementations. Custom Python objectives and unsupported combinations must retain a host path.

Resolve duplicated objective capability tables by consuming or requesting the authoritative native registry. Define deterministic tie-breaking and numerical precision rules. Explicitly handle Float32 near ties and provide a conservative host-scan fallback contract. Avoid one synchronization per candidate or node. Return bounded frontier or completed-tree decisions to the host.

Make the interfaces callable by Task 01 without reimplementing trainer state. Document every required central call and unsupported combination in the handoff.


======================================================================
TASK 04: CONNECT MULTICLASS BATCHING, HYBRID SCHEDULING, AND APPLE HISTOGRAM POLICY
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/gpu_multiclass_batch.mojo, src/mojoboost/hybrid_leaf_scheduler.mojo, src/mojoboost/apple_histogram_policy.mojo, src/mojoboost/gpu_tiling.mojo, and handoffs/connect_04_gpu_scheduling.md. Do not edit central trainers, device policy, generic histograms, objectives, Tree, bindings, Python, tests, or benchmarks.

Make these modules one coherent scheduling-policy layer. Multiclass batching must share bin/row reads where possible while preserving one tree per class per boosting iteration and current serialized output order. Hybrid scheduling must decide whether a leaf stays on GPU, moves to CPU, or uses histogram reuse without changing split semantics. Apple histogram policy must select bounded launch geometry, tile shape, shared-memory layout, partial-buffer budget, and specialization from actual device capability records rather than a hardcoded local Mac.

With no measured crossover artifact, default policies must remain conservative and explainable. Remove duplicate threshold and geometry logic within owned files. Expose pure policy decisions and descriptors for Task 01 and Task 05. Do not advertise speedups or enable experimental level-wise growth by default.


======================================================================
TASK 05: CONNECT DEVICE POLICY, APPLE POLICY, UNIFIED MEMORY, AND INITIALIZATION
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/device.mojo, src/mojoboost/device_policy.mojo, src/mojoboost/apple_gpu_policy.mojo, src/mojoboost/unified_memory_policy.mojo, src/mojoboost/initialization.mojo, python/mojoboost/device_selection.py, python/mojoboost/diagnostics.py, and handoffs/connect_05_device_policy.md. Do not edit trainers, estimator classes, bindings, GPU kernels, packaging, tests, or benchmarks.

Create one authoritative Mojo policy path for capability detection, explicit device requests, auto selection, memory strategy, initialization phases, and explainable decisions. Eliminate duplicate decision engines between Python and Mojo. Python should translate input metadata and format native decision records, not choose the device independently.

Explicit device="gpu" must either run a supported GPU path or return a clear error; never silently fall back. Auto may choose CPU for small or unsupported workloads and GPU only from validated versioned crossover evidence. With no evidence, CPU is the safe choice and the reason must be reported. Account for objective, dense/sparse/categorical support, rows, features, bins, classes, estimated memory, device capabilities, and compilation/session state.

Connect unified-memory strategy and initialization phases to the policy contract without claiming zero-copy. Define reusable context and buffer lifetimes, explicit-copy versus shared/mapped eligibility, page-migration risks, resource cleanup, and diagnostic phase records. Give Task 01 and Task 06 exact native interfaces through the handoff.


======================================================================
TASK 06: CONNECT GPU PREDICTION AND VALIDATION THROUGH BINDINGS TO PYTHON
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/gpu_predict.mojo, src/mojoboost/gpu_validation.mojo if present or needed, bindings/_mojoboost.mojo, bindings/gpu_prediction.mojo if present, and handoffs/connect_06_gpu_prediction_bindings.md. Do not edit estimator classes, public exports, central GPU trainer, serialization, generic metrics, tests, benchmarks, or packaging.

Connect native GPU prediction to real binding entry points for dense batch prediction, response/probability transforms, raw scores, iteration slicing, leaf indices where implemented, and validation prediction reuse. Preserve multiclass shapes, missing and categorical routing, feature count checks, and model ownership. Design handles or scoped objects so validation matrices and prediction buffers can remain device-resident across rounds without exposing unsafe device pointers to Python.

Expose capability and error records needed by estimators. Explicit GPU prediction must fail clearly for unsupported sparse or model modes. Do not silently call CPU while reporting GPU. Keep Python data conversion thin and reuse existing dense-array boundary conventions.

Document exact estimator calls Task 07 must add. Do not duplicate device selection in bindings.


======================================================================
TASK 07: CONNECT ALL ESTIMATOR FEATURES AND PUBLIC PYTHON EXPORTS
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is python/mojoboost/__init__.py, python/mojoboost/basic.py, python/mojoboost/_public_api_plan.py, python/mojoboost/_public_api.py if present, python/mojoboost/callback.py, python/mojoboost/_eval.py, and handoffs/connect_07_python_public.md. Do not edit CV, Dask, inspection, device_selection, diagnostics, bindings, native source, tests, packaging, or workflows.

Connect every already-real Python capability into one coherent public API. Export CV and CVBooster when their real implementation is usable. Expose Dataset, Booster, train, callback factories, diagnostics, device explanation, and inspection data helpers through stable locations without heavy optional imports. Keep Dask lazy and experimental until its real backend exists.

Wire estimator predict and predict_proba device arguments to Task 06's binding interface when available. Wire validation and early stopping to device-resident prediction only through supported native calls. Consolidate aliases and reject conflicts. Ensure regression, binary, multiclass, ranker, dense, sparse, categorical, sample weights, class weights, eval sets, callbacks, init_model, custom objectives/metrics, and prediction modes either reach implementation or raise explicitly instead of ignoring arguments.

Complete fitted attributes including objective_, best_score_, booster_, device_, feature names, class metadata, feature importances, and iteration state where existing native information is available. Keep top-level import cheap and optional-dependency-safe. If a provider interface is unavailable during this parallel task, add the exact intended call behind a narrowly guarded integration seam or document the exact patch rather than fabricating results.


======================================================================
TASK 08: CONNECT THE NATIVE OBJECTIVE AND METRIC REGISTRY EVERYWHERE
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/objective_registry.mojo, src/mojoboost/objective.mojo, src/mojoboost/metrics.mojo, src/mojoboost/custom_metric.mojo, and handoffs/connect_08_objective_registry.md. Do not edit trainers, GPU objective kernels, Python, bindings, parameters, tests, or benchmarks.

Make objective_registry.mojo authoritative instead of dead code. Connect objective name/alias resolution, metric compatibility, direction, transforms, gradient/Hessian capability, GPU eligibility, weighting rules, initialization, and parameter domains into objective.mojo, metrics.mojo, and custom_metric.mojo. Remove or quarantine duplicate registries inside owned files.

Cover all genuinely implemented regression, classification, multiclass, count/distribution, and ranking objectives and metrics. Do not register placeholders. Ensure sample weights, class weights, scale_pos_weight, is_unbalance, per-class weights, custom objectives, custom metrics, and multiple metrics compose without duplicate application.

Expose a narrow declarative query interface for bindings, Python, and GPU providers. Give exact requests to Tasks 03, 07, and 14 in the handoff.


======================================================================
TASK 09: CONNECT EFB AND ALL EXTRA TREE PARAMETERS TO REAL TREE GROWTH
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/efb.mojo, src/mojoboost/tree_parameters_extra.mojo, src/mojoboost/sampling.mojo, src/mojoboost/params.mojo, src/mojoboost/binning.mojo, src/mojoboost/tree.mojo, src/mojoboost/split.mojo, src/mojoboost/categorical.mojo, src/mojoboost/boosting.mojo, and handoffs/connect_09_algorithms.md. Do not edit sparse tree growth, GPU files, bindings, Python, serialization, tests, or benchmarks.

Connect every sound existing tree parameter to the actual dense CPU split/grow path and parameter validation. This includes min_gain_to_split, min_sum_hessian_in_leaf, max_delta_step, path_smooth, extra_trees, feature penalties, monotone penalty/method options, categorical thresholds and regularization, forced-split data structures where representable, feature prefilter behavior, row bagging variants, per-tree/per-level/per-node feature sampling, and deterministic seeds.

Connect EFB end to end behind an explicit disabled-by-default option: conflict measurement, bundle construction, encoded bins, histogram use, split recovery to original feature identity, missing/zero semantics, feature names, and unbundled fallback. Do not enable automatic EFB without later benchmarks. Do not expose parameters that remain inert.

Preserve leaf-wise growth and default-model behavior. Document binding, Python, sparse, GPU, and serialization follow-ups exactly.


======================================================================
TASK 10: CONNECT SPARSE AND CATEGORICAL TRAINING ACROSS CPU AND GPU
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/sparse.mojo, src/mojoboost/histogram_sparse.mojo, src/mojoboost/tree_sparse.mojo, src/mojoboost/boosting_sparse.mojo, src/mojoboost/model_sparse.mojo, src/mojoboost/gpu_sparse.mojo, src/mojoboost/gpu_sparse_layout.mojo, src/mojoboost/gpu_categorical.mojo, and handoffs/connect_10_sparse_categorical.md. Do not edit dense tree growth, generic parameters, bindings, Python adapters, serialization, central GPU trainer, tests, or benchmarks.

Unify the sparse CPU path and existing GPU sparse/categorical primitives behind coherent capability and training interfaces. Preserve CSR/CSC ownership, implicit zero versus missing semantics, explicit zeros, canonical indices, duplicate handling, active rows, bagging/GOSS weights, categorical sets, unknown categories, default directions, split recovery, and deterministic tie-breaking. Never densify silently.

Connect extra tree-parameter semantics already available to sparse growth where valid. Prepare GPU sparse and categorical paths for the central trainer without falsely reporting support. Explicit GPU requests for unsupported sparse/categorical combinations must produce clear errors through capability records.

Give Tasks 01, 06, 07, 09, and 14 exact cross-lane changes in the handoff.


======================================================================
TASK 11: CONNECT SERIALIZATION, SPLIT GAINS, INSPECTION, AND MODEL EDITING DATA
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/serialize.mojo, src/mojoboost/inspection.mojo, src/mojoboost/importance.mojo, python/mojoboost/inspection.py, and handoffs/connect_11_serialization_inspection.md. Do not edit Tree definitions, trainers, public exports, bindings, tests, packaging, or benchmarks.

Connect native inspection rather than leaving it without a caller while Python reparses model text independently. Make serialization preserve split gains, counts/covers, missing directions, category sets, leaf values, objective/class metadata, feature names, iteration grouping, base scores, and every field needed for identical prediction and stable inspection. Preserve backward reading of existing model versions and define a new version only when required.

Create one stable native dump schema consumed by thin Python conversion for dump_model, trees_to_dataframe, split-value histogram data, feature importance, and safe leaf-output inspection. Avoid mandatory plotting dependencies. Model editing methods should be exposed only if invariants, serialization, and prediction remain coherent; otherwise return an explicit unsupported status.

Keep GPU-trained models device-independent and CPU-loadable. Give binding and public-export requests to Tasks 06, 07, and 14.


======================================================================
TASK 12: CONNECT DATASET INPUTS, PREPARED DATA, CV, AND CONTINUED TRAINING
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/trainset.mojo, src/mojoboost/raw_data.mojo, python/mojoboost/_arrays.py, python/mojoboost/cv.py, and handoffs/connect_12_dataset_cv.md. Do not edit estimators, public exports, Booster, bindings, EFB, sparse trainers, serialization, Dask, tests, or benchmarks.

Connect raw_data.mojo and Dataset preprocessing to real dense and sparse ownership rather than leaving alternate ingestion code unused. Consolidate feature names, categorical metadata, labels, weights, groups, init scores, binning state, reference datasets, subsets, and prepared-data reuse. Prevent validation and cross-validation leakage.

Connect cv and CVBooster fully over the existing Dataset/Booster contract: explicit folds, sklearn splitters when installed, shuffled/stratified folds, group-safe ranking folds, multiple metrics, custom metrics, callbacks, early stopping consensus, return_cvbooster, prediction aggregation, and stable mean/std histories. Keep orchestration in Python and model work native. Avoid CPU oversubscription.

Define prepared-table serialization separately from model serialization only where the existing implementation can support it. Give exact binding, estimator, and public-export requests in the handoff.


======================================================================
TASK 13: CONNECT REAL DISTRIBUTED TRANSPORT TO DISTRIBUTED TRAINING
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/distributed_transport.mojo, src/mojoboost/distributed.mojo, src/mojoboost/collective.mojo, src/mojoboost/collectives.mojo if present, and handoffs/connect_13_distributed.md. Do not edit ordinary CPU/GPU trainers, Dask, bindings, Python, tests, workflows, or packaging.

Connect the existing distributed transport to actual distributed training rather than a local mock. Establish real rank/world configuration, schema negotiation, partition metadata, histogram aggregation, global split agreement, deterministic reduction order, objective/metric aggregation, ranking-group constraints, timeouts, cancellation, failure propagation, worker loss, checkpoint boundaries, and shutdown.

Unify duplicate collective abstractions inside owned files. Start with CPU data parallelism. Do not claim multi-host capability unless the implemented transport truly opens and coordinates real processes; if the transport remains incomplete, make the unavailable state explicit and connect every sound stage without marketing it as distributed.

Expose one narrow runtime interface for bindings and Dask. Document hermetic two-process and multi-host validation commands but run none.


======================================================================
TASK 14: CONNECT BINDINGS FOR ALL NATIVE CAPABILITIES EXCEPT GPU PREDICTION
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is bindings/basic_bindings.mojo, bindings/dataset_bindings.mojo, bindings/inspection_bindings.mojo, bindings/distributed_bindings.mojo, bindings/objective_bindings.mojo, or equivalently named new binding modules, plus handoffs/connect_14_bindings.md. Task 06 exclusively owns bindings/_mojoboost.mojo, so do not edit it. Do not edit native implementation, Python, tests, packaging, or workflows.

Create narrow binding modules for capabilities already implemented or being connected: objective/metric registry queries, Dataset metadata and prepared-data operations, inspection structured data and split gains, extra parameters, EFB configuration, distributed runtime capability, model format metadata, device explanation records, and initialization diagnostics.

Do not duplicate algorithms or parse model text in bindings. Validate dimensions, ownership, lifetimes, integer ranges, optional values, and exception translation at the boundary. Do not expose unsafe raw pointers or device buffers to Python.

Produce an exact import-and-export patch for Task 06 to apply to bindings/_mojoboost.mojo. If a provider contract is not yet stable, define a narrow adapter signature and record it rather than fabricating behavior.


======================================================================
TASK 15: CONNECT DASK TO THE REAL DISTRIBUTED BACKEND
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is python/mojoboost/dask.py, python/mojoboost/_dask_runtime.py if present or needed, and handoffs/connect_15_dask.md. Do not edit native distributed code, public exports, estimators, Dataset, bindings, tests, packaging, or workflows.

Connect the Dask estimators to the narrow real distributed runtime contract from Tasks 13 and 14. Preserve Dask Array/DataFrame partitions without gathering the training dataset on the client. Connect categorical metadata, ranking groups, validation, early stopping, model assembly, worker-local runtime lifecycle, cancellation, worker failures, and distributed prediction.

Dask must remain optional and import-safe. Do not simulate distributed training in Python and do not silently fit on the client. When the native backend is unavailable, raise DistributedNotAvailable with a precise reason. Code against a small adapter protocol so this lane remains parallel with Task 13.

Give Task 07 an exact lazy public-export request.


======================================================================
TASK 16: CONNECT LIGHTGBM MODEL INTEROP WITHOUT WEAKENING NATIVE FORMAT
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/lgbm_model_io.mojo, python/mojoboost/lgbm_model_io.py if present or needed, and handoffs/connect_16_lgbm_interop.md. Do not edit native serialization, inspection, Tree, bindings entry point, public exports, tests, or documentation outside the handoff.

Connect the existing LightGBM model-file parser/exporter to a narrow experimental API only where semantics can be represented exactly by MojoBoost Tree and objective metadata. Handle numerical splits, categorical sets, default directions, leaf values, tree/class order, base scores, and supported objective metadata. Reject unsupported LightGBM constructs explicitly rather than approximating them.

Do not make LightGBM format the native persistence format. Do not claim arbitrary model compatibility. Reuse native inspection/model construction contracts and document exact binding and public-export needs. Clearly quarantine this API as experimental until later differential fixtures are run.


======================================================================
TASK 17: CONNECT ALTERNATE BOOSTING MODES ONLY WHERE IMPLEMENTATIONS EXIST
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/boosting_dart.mojo, src/mojoboost/boosting_rf.mojo, src/mojoboost/linear_tree.mojo, src/mojoboost/cegb.mojo, src/mojoboost/alternate_boosting.mojo if needed, and handoffs/connect_17_alternate_boosting.md. Do not edit production boosting.mojo, Tree, params, serialization, bindings, Python, tests, or GPU source.

Inventory existing DART, random-forest boosting, linear-tree, and CEGB implementations. Connect duplicated fragments within owned files into one coherent experimental interface only when an actual algorithm exists. Do not create placeholders merely because parity lists the feature.

Define invariants, seeds, normalization, penalties, model representation, objective compatibility, multiclass/ranking behavior, constraints, continued training, and serialization requirements. If current Tree cannot represent linear leaves, keep linear trees disconnected and state the required model-format change. Produce exact production, parameter, serialization, binding, and Python integration requests.


======================================================================
TASK 18: CONNECT RELEASE PACKAGING, PLATFORM MATRIX, AND INSTALL EXPERIENCE
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is python/pyproject.toml, python/setup.py, python/MANIFEST.in if present, packaging/, .github/workflows/release-macos.yml, .github/workflows/release-linux.yml, docs/INSTALLATION.md, docs/PLATFORM_MATRIX.md, and handoffs/connect_18_release.md. Do not edit runtime source, Python library modules, tests, benchmarks, README, or other workflows.

Connect the built extension, runtime libraries, metadata, wheel tags, platform matrix, and release workflows into a truthful pip-installable artifact design. Ensure macOS ARM64 wheels include required redistributable runtime libraries with correct Mach-O paths. Prepare Linux x86_64/ARM64 only where Mojo and ABI requirements are real. Do not label wheels pure, universal, or manylinux without justification.

Connect version source, package data, license files, Requires-Python, architecture tags, hashes, provenance, TestPyPI trusted publishing, clean-install smoke scripts, and expected unsupported-platform failures. Do not publish or build. Keep production publication gated until later clean-install evidence exists.


======================================================================
TASK 19: CONNECT DOCUMENTATION AND PARITY CLAIMS TO ACTUAL REACHABILITY
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is README.md, python/README.md, docs/LIGHTGBM_PARITY.md, docs/CAPABILITY_LEVELS.md, docs/ARCHITECTURE.md, docs/INTEGRATION_INVENTORY.md, tools/check_parity.py, tools/audit_integration.py, and handoffs/connect_19_docs_parity.md. Do not edit implementation, bindings, tests, packaging, workflows, or other docs.

Update the architecture, integration inventory, public claims, and parity matrix by statically tracing real entry points through bindings to native calls. Distinguish implemented, imported, called, publicly reachable, focused-tested, differential-tested, hardware-validated, and release-packaged. Do not convert another lane's new code to supported without evidence.

Identify every remaining orphan module, duplicate policy, disconnected public parameter, and stale claim after inspecting the live concurrent work. Make tools statically check evidence paths and imports without importing or building the package; do not run them. Keep the README compelling but honest about alpha status, pip availability, Apple GPU evidence, and production maturity.


======================================================================
TASK 20: CONNECT PORTABLE CUDA/AMD SPECIALIZATION POINTS TO SHARED GPU CODE
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is src/mojoboost/gpu_portability.mojo, src/mojoboost/gpu_backend_policy.mojo if present or needed, docs/GPU_PORTABILITY.md, docs/NVIDIA_GPU.md, docs/AMD_GPU.md, and handoffs/connect_20_gpu_portability.md. Do not edit production kernels, Apple policy, trainers, bindings, Python, tests, packaging, or benchmarks.

Connect existing shared GPU abstractions to explicit Metal/CUDA/AMD capability and specialization contracts. Identify Apple-only assumptions, subgroup width assumptions, atomic requirements, shared-memory limits, launch geometry, synchronization, allocation, and numerical behavior. Provide narrow policy interfaces shared kernels can consume while allowing backend-specific specialization.

Do not claim NVIDIA or AMD validation. Unsupported builds and devices must fail clearly. Give Tasks 01, 04, 05, 06, and 18 exact integration requests.


======================================================================
TASK 21: CONNECT C ABI, CLI, AND NATIVE INFERENCE ARTIFACTS
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is capi/, cli/, packaging/native/, docs/C_API.md, docs/CLI.md, and handoffs/connect_21_native_interfaces.md. Do not edit MojoBoost core source, Python, bindings, tests, general packaging, workflows, or README.

Connect the existing C ABI and CLI to the current model format and native prediction/training interfaces. Define ABI version queries, ownership and free functions, error retrieval, dense prediction, model load/save, feature count, and safe failure behavior. Keep the CLI a MojoBoost-native command interface; do not reproduce LightGBM's deprecated config-file surface.

Prepare native artifact layout, headers, library names, loader paths, and distribution requirements without building them. Do not expose incomplete training modes. Document every required core export and release-packaging request.


======================================================================
TASK 22: STATIC CONNECTION AUDIT AND FINAL CROSS-LANE PATCH QUEUE
======================================================================

THIS IS A COMPLETE STANDALONE PROMPT. Work directly in the mojoboost repository in the current shared worktree. Inspect git status, existing implementations, imports, callers, exports, and relevant handoffs before editing. Preserve all concurrent work. Stay strictly inside the exclusive ownership below; never revert, overwrite, clean, stage, commit, push, or reformat another lane's files. If another file must change, record an exact patch request in your handoff instead of editing it.

DO NOT RUN OR WRITE TESTS. Do not run Mojo, Python, Pixi, pip, builds, wheels, examples, benchmarks, profilers, CI, Docker, formatters, linters, network commands, retry loops, polling loops, watchers, or background processes. Static inspection only with tools such as rg, sed, wc, git status, git diff, and git diff --check. Do not claim correctness, performance, parity, packaging, or hardware validation.

FUSE AND CONNECT EXISTING WORK. Before adding code, inventory all existing implementations of this capability. Select one authoritative implementation and connect it to the real production/public call path. Do not create another isolated module, duplicate registry, alternate policy engine, alternate trainer, or parallel model representation. An import or export alone is not integration: real state and parameters must reach the implementation, its output must affect behavior, unsupported cases must fail clearly, and required model state must serialize. Remove obsolete duplicate glue inside owned files only when safe; otherwise quarantine and document it. Preserve the established path behind a conservative fallback or internal strategy switch until later validation.

Keep Mojo authoritative for training, prediction, objectives, metrics, data representation, trees, serialization, and device policy; Python is thin API glue. At completion, write the owned handoff with implementations found, call path before and after, connections completed, duplicates fused or quarantined, remaining disconnections, exact cross-lane patch requests, fallbacks preserved, serialization/public-API effects, risks, and the smallest later focused commands clearly marked UNRUN. Do not commit.

Your exclusive ownership is tools/connectivity_audit.py, docs/CONNECTION_AUDIT.md, and handoffs/connect_22_audit.md. Do not edit any implementation, bindings, public API, tests, packaging, workflows, or other documentation. Author the audit but do not execute it.

Create a static reachability auditor that starts from Python exports, bindings exports, native package exports, C ABI, and CLI entry points and follows imports/references to major modules. It should report likely orphan modules, modules imported but never called, duplicate registries/policies, public parameters with no downstream consumer, binding functions with no Python caller, Python APIs with no native call, serialization fields that are written but not read or read but not written, and parity evidence paths that do not exist.

Read all connect_01 through connect_21 handoffs that exist by the time this lane finishes. Build one cross-lane patch queue assigning every unresolved central edit to its owner. Do not make those edits. Separate true dead code, intentionally experimental code, and code awaiting validation.


======================================================================
AFTER THE PARALLEL ROUND
======================================================================

Stop every coding session. Do not start another feature round immediately. Use one coordinator to read handoffs/connect_01 through connect_22, inspect the live worktree, resolve cross-lane requests in central files, and remove duplicate integration seams.

Only after coordination should focused validation begin. Do not run the full suite by default. Start with static checks, one tiny check per changed subsystem, a clean wheel smoke check, tiny CPU/GPU differential checks, and one reproducible end-to-end Apple benchmark. Broad suites belong on deliberately scheduled CI or controlled hardware, not every coding session.
