% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{mojoboost}
\alias{mojoboost}
\title{Train a mojoboost model in one call}
\usage{
mojoboost(
  data,
  label,
  params = list(),
  nrounds = 100L,
  weight = NULL,
  objective = NULL
)
}
\arguments{
\item{data}{A numeric matrix or data frame.}

\item{label}{Labels, one per row.}

\item{params}{A named list of parameters.}

\item{nrounds}{Number of boosting iterations.}

\item{weight}{Optional per-row weights.}

\item{objective}{Shorthand for `params$objective`. Giving both is an error.}
}
\value{
An object of class `mb.Booster`.
}
\description{
The counterpart of `lightgbm::lightgbm()`: the same thing as
[mb.train()] without building an [mb.Dataset()] first.
}
\examples{
X <- matrix(rnorm(200), ncol = 2)
y <- as.numeric(X[, 1] > 0)
model <- mojoboost(X, y, objective = "binary", nrounds = 20L)
}
