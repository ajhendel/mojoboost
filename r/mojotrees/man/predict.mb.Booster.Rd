% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/booster.R
\name{predict.mb.Booster}
\alias{predict.mb.Booster}
\title{Predict with a mojotrees model}
\usage{
\method{predict}{mb.Booster}(object, newdata, type = c("response", "raw", "class"), ...)
}
\arguments{
\item{object}{An `mb.Booster` from [mb.train()] or [mb.load()].}

\item{newdata}{A numeric matrix or data frame with the same columns, in the
same order, as the training data.}

\item{type}{One of:
\describe{
  \item{`"response"`}{the response scale: probabilities for `binary` and
    `multiclass`, expected counts for `poisson`, the predicted value for
    the regression objectives. The default.}
  \item{`"raw"`}{the ensemble output before any link function, which is
    LightGBM's `rawscore`.}
  \item{`"class"`}{the predicted class index, `0`-based. Requires a
    classification objective.}
}}

\item{...}{Unused, present for S3 compatibility.}
}
\value{
For a single-output model, a numeric vector with one value per row.
  For multiclass with `type` `"response"` or `"raw"`, a matrix with one row
  per observation and one column per class. For `type = "class"`, an
  integer vector.

  Class indices are `0`-based codes, not the original labels: mojotrees's
  model format does not store a label mapping, so translating them back is
  the caller's job.
}
\description{
Predict with a mojotrees model
}
\examples{
X <- matrix(rnorm(200), ncol = 2)
y <- as.numeric(X[, 1] > 0)
model <- mojotrees(X, y, objective = "binary", nrounds = 20L)
probability <- predict(model, X)
log_odds <- predict(model, X, type = "raw")
}
