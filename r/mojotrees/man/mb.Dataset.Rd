% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{mb.Dataset}
\alias{mb.Dataset}
\title{Bundle a matrix with its label and weights}
\usage{
mb.Dataset(data, label, weight = NULL, colnames = NULL)
}
\arguments{
\item{data}{A numeric matrix, one row per observation. `NA` and `NaN` mark
missing values; infinities are rejected.}

\item{label}{A numeric vector with one value per row.}

\item{weight}{Optional per-row weights: finite, nonnegative, not all zero.}

\item{colnames}{Optional feature names, defaulting to `colnames(data)`.}
}
\value{
An object of class `mb.Dataset`.
}
\description{
The counterpart of `lightgbm::lgb.Dataset()`, and the same thing to reach
for when you want the training inputs validated once and carried around
together.
}
\details{
One difference worth knowing: `lgb.Dataset` is a handle to a binned dataset
held by the LightGBM engine, and constructing it does real work. An
`mb.Dataset` is a plain R object. mojotrees bins inside training, so there
is nothing to construct early and nothing engine-side to free. Building one
only validates and stores.
}
\examples{
X <- matrix(rnorm(100), ncol = 2)
y <- X[, 1] * 2 + rnorm(50, sd = 0.1)
ds <- mb.Dataset(X, label = y)
}
