DESTINATION: Modular Discord, the channel for Mojo projects, showcases, or GPU development

I just released MojoBoost 0.1.0a1, an experimental gradient-boosted tree library written natively in Mojo.

The core idea is a LightGBM-familiar Python API with one Mojo implementation spanning CPU and supported GPUs, with an emphasis on the Metal GPU inside Apple Silicon Macs.

It already includes end-to-end dense and sparse training, numerical and categorical features, missing values, regression, classification, ranking, early stopping, callbacks, serialization, TreeSHAP contributions, and an end-to-end GPU tree-growth path. Metal correctness has been exercised on an Apple M4; NVIDIA and AMD are explicitly unvalidated.

The first self-contained wheel is live for CPython 3.14 on Apple Silicon with macOS 26 or newer:

pip install mojoboost

I am looking for technical feedback, reproducible bug reports, Apple hardware results, and contributors—especially around persistent GPU execution, additional wheel targets, broader real-data benchmarks, and cross-device validation.

https://github.com/mojoboost-ml/mojoboost
https://pypi.org/project/mojoboost/

This is an experimental public alpha, not a production or performance-parity claim.
